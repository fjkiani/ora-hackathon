# Instructions

During you interaction with the user, if you find anything reusable in this project (e.g. version of a library, model name), especially about a fix to a mistake you made or a correction you received, you should take note in the `Lessons` section in the `.cursorrules` file so you will not make the same mistake again. 

You should also use the `.cursorrules` file as a scratchpad to organize your thoughts. Especially when you receive a new task, you should first review the content of the scratchpad, clear old different task if necessary, first explain the task, and plan the steps you need to take to complete the task. You can use todo markers to indicate the progress, e.g.
[X] Task 1
[ ] Task 2

Also update the progress of the task in the Scratchpad when you finish a subtask.
Especially when you finished a milestone, it will help to improve your depth of task accomplishment to use the scratchpad to reflect and plan.
The goal is to help you maintain a big picture as well as the progress of the task. Always refer to the Scratchpad when you plan the next step.

# Tools

Note all the tools are in python. So in the case you need to do batch processing, you can always consult the python files and write your own script.

## Screenshot Verification
The screenshot verification workflow allows you to capture screenshots of web pages and verify their appearance using LLMs. The following tools are available:

1. Screenshot Capture:
```bash
venv/bin/python tools/screenshot_utils.py URL [--output OUTPUT] [--width WIDTH] [--height HEIGHT]
```

2. LLM Verification with Images:
```bash
venv/bin/python tools/llm_api.py --prompt "Your verification question" --provider {openai|anthropic} --image path/to/screenshot.png
```

Example workflow:
```python
from screenshot_utils import take_screenshot_sync
from llm_api import query_llm

# Take a screenshot
screenshot_path = take_screenshot_sync('https://example.com', 'screenshot.png')

# Verify with LLM
response = query_llm(
    "What is the background color and title of this webpage?",
    provider="openai",  # or "anthropic"
    image_path=screenshot_path
)
print(response)
```

## LLM

You always have an LLM at your side to help you with the task. For simple tasks, you could invoke the LLM by running the following command:
```
venv/bin/python ./tools/llm_api.py --prompt "What is the capital of France?" --provider "anthropic"
```

The LLM API supports multiple providers:
- OpenAI (default, model: gpt-4o)
- Azure OpenAI (model: configured via AZURE_OPENAI_MODEL_DEPLOYMENT in .env file, defaults to gpt-4o-ms)
- DeepSeek (model: deepseek-chat)
- Anthropic (model: claude-3-sonnet-20240229)
- Gemini (model: gemini-pro)
- Local LLM (model: Qwen/Qwen2.5-32B-Instruct-AWQ)

But usually it's a better idea to check the content of the file and use the APIs in the `tools/llm_api.py` file to invoke the LLM if needed.

## Web browser

You could use the `tools/web_scraper.py` file to scrape the web.
```
venv/bin/python ./tools/web_scraper.py --max-concurrent 3 URL1 URL2 URL3
```
This will output the content of the web pages.

## Search engine

You could use the `tools/search_engine.py` file to search the web.
```
venv/bin/python ./tools/search_engine.py "your search keywords"
```
This will output the search results in the following format:
```
URL: https://example.com
Title: This is the title of the search result
Snippet: This is a snippet of the search result
```
If needed, you can further use the `web_scraper.py` file to scrape the web page content.

# Lessons

## User Specified Lessons

- You have a python venv in ./venv. Use it.
- Include info useful for debugging in the program output.
- Read the file before you try to edit it.
- Due to Cursor's limit, when you use `git` and `gh` and need to submit a multiline commit message, first write the message in a file, and then use `git commit -F <filename>` or similar command to commit. And then remove the file. Include "[Cursor] " in the commit message and PR title.

## Cursor learned

- For search results, ensure proper handling of different character encodings (UTF-8) for international queries
- Add debug information to stderr while keeping the main output clean in stdout for better pipeline integration
- When using seaborn styles in matplotlib, use 'seaborn-v0_8' instead of 'seaborn' as the style name due to recent seaborn version changes
- Use 'gpt-4o' as the model name for OpenAI's GPT-4 with vision capabilities
- When using f-strings with JSON templates, double the curly braces `{{` and `}}` to escape them properly and avoid format specifier errors
- When working with experimental models like `gemini-2.0-flash-thinking-exp-01-21`, always implement fallback mechanisms to standard models in case the experimental model is unavailable
- For options data, use RapidAPI directly instead of the YahooFinanceConnector class to avoid compatibility issues with the OptionChainQuote initialization
- When processing options data from RapidAPI, create a mapping of strikes to straddles for easier lookup and processing of call and put data
- When creating React components, ensure robust error handling for API calls and data processing to prevent UI crashes
- Use React.lazy and Suspense for code splitting in React applications to improve initial load time
- For TypeScript React projects, define proper interface types for component props to ensure type safety

# Scratchpad

## Current Task: Rebrand Landing Page to DefiKSA.org
[ ] Update branding elements (logo, name) across the landing page
[ ] Refocus messaging for Saudi Arabian (KSA) market
[ ] Review content for alignment with Sharia compliance principles (initial pass)
[ ] Potentially hint at future cross-border payment functionality

## Sub-Tasks:
[X] Identify specific files in `landing/src/` needing updates (e.g., components, assets)
[X] Gather new logo file and specific text/messaging changes from user (Using placeholders for now)
[X] Implement logo replacement (Using text placeholder DK)
[X] Implement text and messaging updates in relevant sections (Hero, Footer, Metadata)
[X] Implement text updates in Agent Showcase title/description
[ ] Implement detailed explanations for Agent Showcase examples
[ ] Update other sections (Features, Tech, Tokenomics) with new branding/messaging
[ ] Adjust styling (colors?) if needed for new brand identity
[ ] Review changes
[ ] Update READMEs

## Rebranding Notes (DefiKSA) - Landing Page Focus

*   **New Brand:** DefiKSA.org (Update site name, logos)
*   **Target Audience:** Saudi Arabia (KSA) - Adjust language, visuals, value proposition. Emphasize security, compliance (where applicable *now*), efficiency.
*   **Messaging:**
    *   Highlight AI-powered finance tailored for the region.
    *   Subtly introduce the concept of secure, efficient financial operations (can bridge to payments later).
    *   Mention ORA framework benefits (transparency, security).
*   **Compliance:** Ensure landing page language doesn't make premature claims but aligns with the *direction* of Sharia compliance. Avoid explicitly mentioning interest-based DeFi mechanisms if possible in marketing copy.
*   **Existing Tech:** Leverage existing descriptions of AI agents, ORA, risk tools, but frame them for the KSA audience.

## Agent Showcase Enhancement Plan:
[ ] Define detailed content (Problem, How it Works, Benefits) for each Sharia-compliant agent example.
[ ] Modify `Agent` interface/data in `AgentShowcase.tsx` to include detailed content.
[ ] Update JSX in `AgentShowcase.tsx` detail panel to display the new detailed content instead of just description/metrics.

---
## Previous Task: Landing Page Implementation (Reference)

### Project Context
[X] We currently have:
[X] 1. A functional application frontend with API integrations, data visualization, and interactive components (Note: Confirmed this is mostly mocked/prototype stage)
[X] 2. Plans for a whitepaper that will detail our project's vision, technology, and value proposition

[X] We need to create a separate landing page focused on marketing and showcasing details from the whitepaper while keeping the application separate.

### Project Background
[X] - 🏆 NYU Hackathon Winner
[X] - Innovative integration of AI and blockchain technologies through the ORA Framework
[X] - Bridges traditional finance and decentralized finance (DeFi)
[X] - AI agents autonomously interact with blockchain networks to execute financial strategies
[X] - Comprehensive dashboard for monitoring DeFi protocols, assessing risks, and creating AI agents

### Key Features (Landing Page Representation)
[X] - Interactive ORA Framework Visualization with animations
[X] - Risk Assessment Tools for DeFi protocols
[X] - Protocol Monitoring Dashboard for real-time metrics
[X] - Agent Creation Interface for AI agent deployment
[X] - Transaction Logs for agent-executed transactions
[X] - Claude AI Assistant for DeFi questions

### Technical Approach (Landing Page)
[X] - Frontend: React with TypeScript for frontend (Next.js confirmed in `landing/`)
[X] - Styling: Tailwind CSS
[X] - SEO: Static site generation benefits

### Project Structure (Confirmed)
```
crypto-hedge-fund-main/
├── landing/            # New landing page code (this implementation)
│   ├── public/         # Static assets (favicon, images, potential logo location)
│   ├── src/            # Source code
│   │   ├── app/        # Next.js app router (pages)
│   │   ├── components/ # Reusable UI components (sections live here)
│   │   └── data/       # Data files (e.g., whitepaper)
│   ├── next.config.js  # Next.js config
│   ├── package.json
│   └── tailwind.config.js
├── ... (other project files/dirs like contracts/, Server/, etc.)
```

### Implementation Progress (Marked as complete for now)
[X] Phase 1: Setup & Design
[X] Phase 2: Core Development
[X] Phase 3: Enhancement & Integration
[X] Phase 4: Testing & Deployment (Assumed complete for initial version)

### Key Components Implemented (Reference for Update)
[X] HeroSection
[X] FeaturesSection
[X] TechnologySection
[X] TokenomicsSection (May need significant review/update for Sharia compliance later)
[X] RoadmapSection
[X] ContactSection
[X] WhitepaperSection
[X] MobileMenu
[X] AboutUsSection (Was pending, needs check)

### Next Steps (Previous Task - Now Superseded)
[ ] Create AboutUsSection highlighting NYU Hackathon win -> Check if exists, update with KSA focus
[ ] Implement newsletter signup functionality -> Check if exists
[ ] Optimize images and assets for performance -> Review after rebranding
[ ] Add analytics integration -> Future task
[ ] Prepare for deployment -> After rebranding updates
